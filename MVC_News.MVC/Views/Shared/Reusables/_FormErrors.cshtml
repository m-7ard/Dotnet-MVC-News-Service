@model List<string>?

@if (Model is null)
{

}
else
{
    <div class="flex flex-col px-4 py-2 border border-gray-900 bg-red-300">
        @for (var i = 0; i < Model.Count; i++)
        {
            <div class="text-sm">
                &#8226 @Model[i]
            </div>
        }
    </div>
}